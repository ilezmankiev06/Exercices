// Copy and paste your function `squareNumber` here from exercise one

export function squareNumber(number: number): number {
    for (let i = 0; i <= number; i++){
        const result = number * number;
        return result
}
}
